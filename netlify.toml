[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  node_bundler = "nft"

# Synthesize needs more time — use Netlify's 26s max for serverless
[functions."___netlify-server-handler"]
  node_bundler = "nft"
